apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.discountSegmentsService.name }}
  labels:
    app: {{ .Values.discountSegmentsService.name }}
spec:
  selector:
    app: {{ .Values.discountSegmentsService.name }}
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
