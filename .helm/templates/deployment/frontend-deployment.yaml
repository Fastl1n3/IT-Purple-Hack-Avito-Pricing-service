apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontend.name }}
  labels:
    app: {{.Values.frontend.name}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{.Values.frontend.name}}
  template:
    metadata:
      labels:
        app: {{.Values.frontend.name}}
      annotations:
        checksum/env-config: {{ include (print $.Template.BasePath "/configmap/frontend-env-configmap.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{.Values.frontend.name}}
          image: {{.Values.frontend.image.repository}}:{{.Values.frontend.image.tag}}
          envFrom:
            - configMapRef:
                name: {{.Values.frontend.name}}-environment
          ports:
            - containerPort: {{.Values.frontend.service.port}}
