apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.logstash.name }}-environment
  labels:
    app: {{ .Values.logstash.name}}
data:
  {{- range $key, $val := .Values.logstash.service}}
  {{ $key}}: {{$val | quote}}
  {{- end}}
